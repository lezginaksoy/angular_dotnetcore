export class Transaction {
  id?: string;
  termId?: string;
  lastupdate?: string;
  cardBrand?: string;
  accountNo: string;
  txnCode: string;
  txnSubCode: string;
  rrn: string;
  reqDateTime: string;  
}
